.list
  %h2 Projects
  
  %div
    != show_flash
    != show_error
  
  %p
    %a(href="/projects/new") Make a new project
    
  - if !(@projects.nil?)
    %ul
      - @projects.each do |project|
        %li
          = project.name
          %br
          %a(href="http://#{project.link}")
            = project.link
          %br
          = project.description
          %br
          %a(href="/projects/delete/#{project.id}") Delete
          - if (!project.published?)
            %a(href="/projects/publish/#{project.id}") Publish
          - else
            %a(href="/projects/unpublish/#{project.id}") Unpublish
          %br
  - else
    %p
      = message